## ct:cluster/tp_to_anchor — Macro: TPs the player to the anchor coordinates.
## Reads ct:main clusters[0].anchor and teleports the executor.

function ct:cluster/tp_to_anchor_macro with storage ct:main clusters[0].anchor
